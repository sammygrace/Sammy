<section class="banner">
<hr>
<h1 class="title"><%= @school.try(:name) %> Vendors:</h1>
</section>

<p><%= link_to 'Back to Schools', schools_path %></p>

<%= render 'search_form'  %>

<section class="content">
  <h2 style="color:green;">
    <%== pagy_info(@pagy) %>
  </h2>

  <p><%== pagy_nav(@pagy) %></p>


<table>
  <thead>
    <tr>
      <th>Name</th> 
      <th>Address</th>
      <th>Description</th>
      <th>Content</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody> 
    <%= render @vendors %>
  </tbody>
</table>

</section>

<br>

<%= link_to 'New Vendor', new_vendor_path %>
